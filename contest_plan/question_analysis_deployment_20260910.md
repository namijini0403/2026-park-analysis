# 질문형 분석 배포 검증 — 2026-09-10

기능 코드 커밋: `9c58d01` (주 구현 `0b91ad2`, 공간도 비율 수정 `1e0567e`). 브랜치 `feat/all-school-levels-academy-public-metrics`에 푸시했다.

미리보기: https://education-living-area-preview-production.up.railway.app — 접속 코드 `2026`. 분석 인사이트 탭의 질문 입력창에서 실행한다.

Railway 배포 `b989dc1c-2a75-4337-99c9-0ca0d5cc7d86`에 대해 명시적 프로젝트·환경·서비스로 조회한 최종 상태 `SUCCESS`를 확인했다. 처음 배포의 실서버 비교 질문에서 ‘남동구’를 ‘동구’로 중복 해석하는 오류를 발견하여 긴 지역명을 우선 인식하도록 고쳤고, 회귀 테스트와 재배포 후 같은 질문을 통과했다.

## 검증 결과

- 기존 교육 앱 테스트 6개, 신규 분석 엔진·API·AI 계획기·질문 UI 테스트 4개, Python 데이터 테스트 4건 통과. 최종 변경에 영향받는 API·계획기·UI 테스트를 다시 통과했다.
- 실제 입력 데이터: 917개 기관, 도서관 7,304개 후보와 10개 거리·공급 시나리오, 학원 6개 군집 시나리오. 후보의 인천 경계 포함, 공급 중복 제거, 결측 유지, 반경별 접근 개수 단조성, 군집 시설 보존을 검증했다.
- 실제 배포 주소의 HTML, 질문·네트워크·통계 스크립트 및 신규 JSON 4개, 총 8개 응답이 로컬 원본 SHA-256과 일치했다.
- 실제 POST `/api/analysis`에서 관계, 유치원 원아 추세, 도서관 후보, 학원 군집, 공간 집중, 격차, 연수구·남동구 비교, 네트워크 확산, 도로 중단의 9개 질문이 HTTP 200과 계산 결과·차트·재현 ID를 반환했다.
- 도서관 7,304개 후보와 학원 기본 설정 119개 군집을 실서버 응답에서 확인했다. 미확보 수능 성과 질문은 HTTP 400과 자료 부재 안내, GET은 HTTP 405를 반환했다.
- 관계·공간·도서관·학원 SVG를 렌더링하여 글자, 표시값, 점 및 구역 형상을 확인했다. SVG 내 색상을 RGB로 표현하고 위도에 따른 공간도 비율을 적용했다.

상세 실서버 응답 요약과 파일 해시는 [검증 JSON](question_analysis_live_validation_20260910.json)에 보존했다. 수식·자료 출처·재현 명령은 [방법 문서](question_analysis_methods.md), 범위별 완료 근거는 [확장 기록](question_analysis_expansion.md)에 있다.

## 확인 범위와 한계

미리보기의 실제 질문 해석은 **규칙 기반**으로 검증했다. 외부 AI API 경로는 모의 응답으로 엄격한 스키마·허용 변수 검사·장애 시 대체 동작을 검증했으며, 실제 유료 모델 호출의 품질을 검증했다고 주장하지 않는다. 서버 키를 설정하면 AI가 허용된 계획을 고르며 수치 계산은 계속 서버 분석 엔진이 수행한다.

브라우저 제어 도구에 사용 가능한 브라우저가 없어 전체 화면의 실제 브라우저 시각 검증은 수행하지 못했다. JSDOM 통합 동작, 정적 SVG 시각 확인, 실제 HTTP 검증은 완료했다.

도서관 반경은 계획 접근권역이며 인과적인 교육 효과 반경이 아니다. 수요는 추정 거주 인구이고 후보의 토지·도로·운영 가능성은 미검토다. 인천 경계는 OSM 스냅샷이며 시외 도서관은 미반영이다. 학원 구역은 탐색적 밀도 군집이며 공인 학원가 경계가 아니다. 대입·수능 등 확보하지 못한 성과는 대체 지표로 만들어 넣지 않았다.

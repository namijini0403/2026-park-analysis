# 관측 우선 전환 — 2026-09-12

사용자 승인 원칙: 기본 종합점수·전체 순위 제거, 미확보 판단 보류, 이용·안전·실행 조건 필터, 검증된 대안의 명시적 선호 비교, 근거 부족 시 복수 대안.

## 반영

- 학생 생활서비스 화면을 학교 검색 → 확인된 사실 / 더 확인할 조건 / 함께 검토할 방법의 세 칸으로 변경했다. 모바일에서는 세 칸이 세로로 배치된다. 미래 자료·설치 조사 지점·세부 출처는 기본으로 접혀 있다.
- 60/25/15 가중치, 600명 포화 기준, 20권 목표, 점수 구간, 학교 전체 순위와 신설 우선 분기를 제거했다. JSON과 CSV에도 점수를 저장하지 않는다.
- v3 도달권 대표점 관측을 사용하고 실제 경로 검증과 구분한다. 도서지역 30개교는 별도 검토로 표시한다.
- 운동시설 25곳은 모두 부평구 소재인 부분 목록임을 표시한다. 미관측은 공급 부족으로 처리하지 않는다.
- 대안 비교에는 이용·안전·실행·경로 확인 및 출처가 모두 필요하다. 사용자가 검증된 보행거리 또는 추가 이용규모를 선택할 때만 해당 관측축으로 조건부 정렬한다. 기존 조사 지점은 이 검증을 통과하지 못하므로 실제 조건부 순위는 현재 비어 있다. 값을 임의로 보충하지 않았다.
- 공원·독서 0~3 등급 비교와 동점 공원 우선 선택을 제거하고 두 자원의 검토 조건을 병렬로 제시한다. 12개 규칙 유지율을 기본 추천의 신뢰처럼 표시하지 않는다.
- 독서 중앙값×0.5와 상위25% 분류를 자동 부족·지원방식으로 사용하지 않는다. 장서·사서·시설 관측값을 유지한다.
- 이전 저장 버전 복원 시 자동 추천과 독서 부족 분류가 살아나지 않도록 서버 기동 변환을 추가했다. 원천 측정값은 유지한다.
- 프로젝트 AGENTS.md에 위 철학과 쉬운 기본 화면 원칙을 영구 지침으로 추가했다.

## 확인

정책 데이터 5개 검사, DOM 화면·명시적 선호 비교 검사, 과거 저장 버전 복원 회귀 검사를 통과했다. 미검증 안전 조건을 짧은 거리로 상쇄할 수 없는지 별도 가상 사례로 검사했다. 메인 앱 시작·학교 표시·필터·보고서·AI 문맥 검사도 수행했다. 실제 연결 브라우저가 없어 시각 렌더링 검수는 수행하지 못했다.

## 범위

기존 공원 제출본의 검증된 Case·Pareto·사용자 선택 시나리오는 유지한다. 원본 HWP/PDF는 수정하지 않았다. 현재 구현은 자료를 보이는 단계와 판단 가능한 단계를 분리하며, 확인되지 않은 운동공간을 검증된 후보라고 승격하지 않는다.

## 원격 확인

배포 `64f8bc67-07fb-44ca-b42c-c651f33bf88b` SUCCESS. 운영 서비스 7개 경로의 HTTP 200, 화면·생활서비스 자료 파일 일치, 272개교 점수 제거, 정책 카드 강제 우선안 제거, 독서 미확보 상태 보존을 확인했다. 상세 기록: `observations_first_live_validation_20260912.json`.

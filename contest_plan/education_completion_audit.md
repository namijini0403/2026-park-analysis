# 학교급·학원·학교 실적 확장 완료 점검

> 2026-09-09 재개 점검: Playwright 브라우저 연결에 성공했다. 아래의 '브라우저 없음' 기록은 이전 시점의 상태다. 현재 차단 원인은 기본 지도 JavaScript 키에 로컬 주소가 등록되지 않은 것이다. `http://127.0.0.1:8765`와 `http://localhost:8765`를 Referer로 한 SDK 응답은 모두 HTTP 401 `AccessDeniedError / domain mismatched`였다. 실제 브라우저에서도 SDK 로딩 실패와 학교급 선택 비활성을 확인했다. 키·도메인 설정을 바꾸거나 인증을 우회하지 않았다. 지도·학원·경로 시각 검증은 아직 미완료다. 첫 화면과 활용 가이드에 남아 있던 초등학교 전용 소개는 확장 학교급·학원·공개자료 범위로 수정했다.

점검 기준: 2026-09-09, `feat/all-school-levels-academy-public-metrics`, 점검 전 HEAD `6a7f9d1`.

전체 목표는 **미완료**다. 아래는 사용자의 원래 범위를 현재 산출물과 대조한 결과이며, 확보한 자료만으로 목표를 축소해 완료 처리하지 않는다.

| 요청 | 현재 근거 | 판정과 남은 범위 |
|---|---|---|
| 새 브랜치에서 기존 구조에 레이어 추가 | 현재 브랜치, `assets/education-layers.js`, 기존 `index.html` 연결 | 구현됨. 기존 초등 272교와 확장 645개 기관을 함께 제공 |
| 유치원·중학교·고등학교를 초등 수준의 분석 범위로 확장 | `school_analysis.json`: 유치원 369·중학교 147·고등학교 129 모두 available. 공원 지표와 학원·공사·대단지·도서관·유흥·놀이터·재개발 맥락 포함 | 공통 분석 기능 구현. 실제 지도 시각 검증과 아래 미래 공간 수요 차이는 남아 있음 |
| 현재 격차·접근 마찰·수요·후보 추천 | 500m v3 도달권 645개, 유효 공원 경로 603개, 도로 유형·주거 통행 가정, 학교 중심 연령 인구, 후보 44,332쌍, 정책 12조건 조합 | 원형 도달권 대체 없이 제공. 유효 경로 미확보 42곳은 길을 만들지 않음. 후보지별 실제 이용자·신규 수혜 인원을 검증한 예측은 아님 |
| 같은 학교급 유사기관 비교·미래 지원 | KNN 유치원 366·중학교 145·고등학교 128, 총 639개. 학교별 지원 예측 유치원 361·중학교 144·고등학교 127, 총 632개. 지역 연령 예측은 별도 검증 자료 포함 | 이력 부족은 미산출. 초등 모형 성능이나 SHAP 결과를 다른 학교급에 전용하지 않음. 후보별 개발·이동을 반영한 미래 공간 분포 모형은 미구축 |
| 학교별 공개 실적 조사·연결 | 원장 920개에 공시 파일·학교별 현황표·AI 근거. 진학/취업 276교 549관측, 과학전람회 19교 53관측, 발명 32교 68관측, 전국체육대회 33교 180관측, 운동부 135교 836게시 기록. 장학·동아리·방과후·PAPS 공시 포함 | 확보 자료 연결됨. 서로 다른 관측 단위를 합산하지 않음. 수능 학교별 수치·학업성취 수치·모든 대외수상 전수 확보는 미완료 |
| 학원을 공원처럼 주변 환경에 추가, 가능한 대상 분류 | 학원·교습소 6,839개, 지도 좌표 6,810개, 미확보 29개. 교습과정 기반 유아·초등·중등·고등·중고·통합·복합·미확인 및 별도 예체능 축 | 구현됨. 학원을 학교 분석 대상이나 공공 활동공간으로 취급하지 않음. 학교급이 없는 교습과정은 임의 분류하지 않음 |
| 공개자료가 없는 경우 다른 작업 우선 | 공시 실패/빈 응답 재검토 기록, 학업성취 접근 제한, 신설기관 이력 부족, 좌표 미확보를 원문과 상태로 보존 | 결측을 0이나 부존재로 바꾸지 않음. CAPTCHA가 있는 공개 페이지는 자료가 없다는 뜻이 아니므로 '미수집'으로 구분 |
| 토큰 최적화·효율·결과 품질 | 원본 캐시·입력 해시, 학원만 갱신하는 공통 집계 함수, 질문 주제/연도별 근거 선택, 85개 수상 질문에서 근거 문자 수 중앙값 19.5~32.3% 감소 | 실제 토큰·요금 실측치는 아님. 원자료·공식 출처·결측 의미를 유지한 범위에서 절감 |

## 현재 상태를 직접 확인한 항목

- `school_data_coverage_manifest.json`의 입력 **933개**를 현재 파일과 SHA-256으로 대조해 불일치 0개를 확인했다.
- 원장 920개 중 앱 보고서 대상은 **917개**다. 선택적 전체 보고서 실행 검사에서 초등 272·유치원 369·중학교 147·고등학교 129개를 모두 열어 학교명·공시 건수·공통 실적 영역 및 확장 조정 요소를 확인했다.
- 교육 관련 Python 검사 **69개**, 앱 시작·보고서·AI 검사 및 최근 수정에 해당하는 AI 검사가 통과했다. 실제 지도 SDK·타일은 모의 객체로 대체했으므로 브라우저 시각 검증 완료로 인용하지 않는다.
- 유치원·중·고 원장 ID 집합과 분석 ID 집합은 일치한다. 기존 초등 분석에 없는 인천윤슬초·인천검단호수초·인천달빛초 3교는 원장에만 있다. 이는 확장 645개 기관의 누락과 구분한다.
- 이번 점검에서도 컴퓨터 도구의 응답은 `apps: [], browsers: []`였다. 이전 브라우저 연결 시도도 `No browser is available`을 반환했다. 현재 환경에서 실제 지도 조작·타일 시각 검증은 수행할 수 없다.

## 완료로 인정하지 않은 항목

1. 실제 브라우저에서 지도·필터·학교 선택·경로·후보지·학원 표시를 확인해야 한다. 연결 가능한 브라우저가 필요하다.
2. 학교별 학업성취 개별 공시는 존재하지만 보안문자 요구로 미수집이다. 자동 일괄 목록에서 확보한 항목과 혼동하지 않는다. 수능 수치와 모든 대외수상 이력 역시 현재 산출물로 전수 확보를 증명할 수 없다.
3. 후보지 미래 수요는 지역 성장률을 적용하는 지원 시나리오다. 공개자료로 검증 가능한 미래 공간 분포와 실제 신규 이용자 예측을 확보한 것으로 표현하지 않는다. 이를 단순히 초등 SHAP 결과를 복사하는 것으로 해소할 수 없다.

세부 출처·재현 명령·단계별 검증은 [작업 기록](education_layer_status.md), 실적 확보·보류 근거는 [공개 실적 출처 점검](public_performance_source_audit.md), 학교별 상태는 `data_processed/education/school_data_coverage.csv`를 참조한다.


## 미래 수요 동등성 재점검: 기존 초등 코드와 비교

2026-09-09 추가 정적 점검. 아래는 저장소 코드의 목표값·검증 절차를 읽은 결과이며, 과거 모델을 재학습하거나 실행 중인 배포 모델의 계보를 새로 검증한 결과는 아니다.

| 기존 코드 | 직접 확인한 내용 | 확장에 적용할 판단 |
|---|---|---|
| `scripts/forecasting/run_model2_feature_boost_v3.py:111` 및 `:126` | 2029·2031 목표값은 `iso_child_total × cohort_change_*_prophet`. 학교 행을 무작위 5분할해 이 계산값에 대한 오차를 평가 | 실제 미래 이용자 관측에 대한 시간 외 검증으로 인용할 수 없음. 같은 목표값을 다른 학교급 XGBoost로 재학습하는 작업을 필수 완료 조건으로 삼지 않음 |
| `scripts/forecasting/run_beneficiary_models_v2.py:196` | 앞선 버전도 현재 아동 수와 지역 변화율의 곱을 목표값으로 사용 | 여러 버전이 있다는 사실은 미래 실측 검증 근거가 되지 않음 |
| `scripts/candidate_generation/predict_grid_demand.py:59` | 학교 모형의 후보지 입력을 연결 학교 평균 등으로 구성. `gu_avg_grid_total_pop`도 연결 학교 `iso_child_total` 평균으로 근사 | 학교와 후보지의 관측 단위가 달라 그대로 복제하지 않음 |
| `scripts/candidate_generation/build_mixed_demand_model.py:554` | 공간 보정 입력에 `child_ratio_1km`을 포함하고, 목표도 그 값의 로그. 같은 입력에서 학습·예측하며 이후 부모 격자 내 가중치 합과 총량 보존 검사 | 총량 보존은 검산이지 독립적인 공간 예측 성능 검증이 아님. 이 보정이나 SHAP를 다른 연령에 복사하는 근거로 쓰지 않음 |
| `scripts/education/build_regional_age_forecasts.py:84` 및 `:95` | 확장 모형은 실제 지역 연령 인구를 사용해 2020~2022년 목표로 모형을 선택하고 2023~2025년 목표로 별도 검증. 검증 간격은 1·3년 | 지역 수준의 미래 수요 지원 근거는 확보. 후보지 공간 이동이나 실제 신규 이용자 수 검증으로 확대 해석하지 않음 |

따라서 **지역 수요 전망과 후보지 성장 시나리오 기능은 이미 제공**한다. 앞 표의 '후보별 개발·이동을 반영한 미래 공간 분포 모형 미구축'은 결과 해석의 한계이며, 사용자가 별도로 요구하지 않은 미래 실측자료나 새 AI 모듈을 반드시 확보해야 한다는 추가 요구사항이 아니다. 공개된 관측자료가 없는 실제 신규 이용자 예측은 사용자의 '없는 공개데이터는 건너뛰기' 원칙에 따라 보류한다. 실제 지도 검증과 미수집 공시 상태는 그대로 남긴다.

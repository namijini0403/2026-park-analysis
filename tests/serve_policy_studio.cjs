// Isolated preview; no scheduler/startup rebuild, no production writes.
const http=require('node:http'),fs=require('node:fs'),path=require('node:path');
process.env.AI_EXPLAINER_ENABLED='false';process.env.AI_ANALYSIS_ENABLED='false';
const chat=require('../api/chat'),model=require('../api/_school_summary'),root=path.resolve(__dirname,'..');
const server=http.createServer(async(req,res)=>{try{const u=new URL(req.url,'http://localhost');if(u.pathname==='/api/chat'){let raw='';for await(const c of req){raw+=c;if(raw.length>1000000)throw Error('Request too large');}req.body=JSON.parse(raw);return chat(req,res);}if(u.pathname==='/api/school-summary'){res.setHeader('Content-Type','application/json');return res.end(JSON.stringify(u.searchParams.has('id')?model.summary(u.searchParams.get('id'),u.searchParams.get('kind')):{schools:model.list()}));}if(u.pathname.startsWith('/api/update-center')){req.body={};return require('../api/update-center')(req,res);}const file=path.resolve(root,'.'+decodeURIComponent(u.pathname==='/'?'/index.html':u.pathname));if(!file.startsWith(root+path.sep)||!fs.existsSync(file)||!fs.statSync(file).isFile()){res.statusCode=404;return res.end();}res.setHeader('Content-Type',({'.html':'text/html; charset=utf-8','.css':'text/css','.js':'application/javascript','.json':'application/json','.geojson':'application/json','.png':'image/png'})[path.extname(file)]||'application/octet-stream');fs.createReadStream(file).pipe(res);}catch(e){res.statusCode=400;res.end(JSON.stringify({summary:e.message}));}});
if(require.main===module)server.listen(Number(process.env.POLICY_STUDIO_PORT||43127),'127.0.0.1',()=>console.log('Policy studio preview http://127.0.0.1:'+server.address().port));module.exports=server;

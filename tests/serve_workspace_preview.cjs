const http=require('node:http'),fs=require('node:fs'),path=require('node:path');const root=path.resolve(__dirname,'..');const handlers={'/api/google-places-config':require('../api/google-places-config'),'/api/domain-stats':require('../api/domain-stats'),'/api/school-summary':require('../api/school-summary'),'/api/school-profile':require('../api/school-profile'),'/api/chat':require('../api/chat')};
http.createServer(async(req,res)=>{try{const u=new URL(req.url,'http://localhost');if(handlers[u.pathname]){if(req.method==='POST'){let body='';for await(const b of req)body+=b;req.body=JSON.parse(body);}return await handlers[u.pathname](req,res);}const file=path.resolve(root,'.'+(u.pathname==='/'?'/index.html':u.pathname));if(!file.startsWith(root+path.sep))throw Error('path');res.setHeader('Content-Type',({'.js':'text/javascript','.css':'text/css','.html':'text/html','.json':'application/json','.png':'image/png'})[path.extname(file)]||'application/octet-stream');fs.createReadStream(file).on('error',()=>{res.statusCode=404;res.end();}).pipe(res);}catch(e){res.statusCode=500;res.end(e.message);}}).listen(Number(process.env.PORT||8877),'127.0.0.1',()=>console.log('Workspace QA port '+(process.env.PORT||8877)));


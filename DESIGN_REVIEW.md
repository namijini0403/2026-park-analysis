# 디자인 검토용 브랜치

- 브랜치: `design/night-ivory-review-20260910`
- 비교 기준: `policy-reachability`의 `d8013c8da1b9adb06172dd8497de072dd2f4b5d6`
- 생성 시점의 `railway-deploy`도 동일한 기준 커밋입니다.
- 기존 브랜치에 병합하거나 운영 배포하지 않고, 디자인 변경만 검토하기 위한 작업본입니다.

## 포함한 변경

- 네이비·민트 표지와 지도 작업 화면, 밝은 선택 상태의 지도 필터
- 아이보리 계열의 보고서·시뮬레이션·통계·활용 가이드 읽기 영역
- 반투명 네이비 인증창과 제공된 투명 로고
- 표지 배경 영상과 표지 전용 재생 제어: 처음 6초와 10초 이후는 원래 속도, 지도 접근 구간만 부드럽게 최대 2배속
- 기존 표지 문장의 두 줄 배치, 버튼 강조, 정책 행동 카드 태그의 다음 줄 배치
- 위 변경을 반영한 프런트엔드 빌드 산출물

## 유지한 경계

기준 브랜치의 데이터, API, 서버, 분석 기준, 500m 판정, 학교·도서관 레이어, 인증 로직과 기존 문구는 변경하지 않습니다. HTML의 기존 인라인 스크립트도 그대로 유지합니다. 새 `assets/cover-motion.js`는 표지 영상 표현만 담당합니다.

로컬 통합 미리보기에는 다른 브랜치에서 가져온 기능 변경도 있었으므로, 그 파일을 통째로 복사하지 않고 기준 코드 위에 스타일만 선택적으로 적용했습니다. 따라서 미리보기 전용 지도 연결 실패 안내와 별도 기능 개선은 이 브랜치에 포함하지 않습니다.

## 배포 주의

`vercel.json`에는 이 디자인 브랜치만 자동 배포하지 않도록 `git.deploymentEnabled` 항목을 추가했습니다. 다른 브랜치의 배포 설정은 변경하지 않습니다.

이 브랜치를 `main` 또는 `railway-deploy`에 바로 병합하지 마세요. 기준 브랜치와 변경 파일을 비교한 뒤, 담당자의 확인을 거쳐 필요한 디자인 변경만 통합하면 됩니다. 로컬 검수 기록, 임시 인증 테스트, 개인 파일 경로, API 키와 `.env`는 업로드 대상이 아닙니다.

## 검증

기존 데이터·로직·본문 보존 검사 29개 항목과 Vite 프런트엔드 빌드를 통과했습니다. 실제 `main.tsx` 진입 경로의 TypeScript 오류는 없습니다. 전체 TypeScript 검사에는 기준 브랜치부터 존재한 미사용 legacy 파일 오류 6개가 남아 있으며, 디자인 범위를 넘는 삭제나 기능 수정은 하지 않았습니다.
